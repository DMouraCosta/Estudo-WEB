// repetição por array

const cidades = ['São Paulo', 'Rio de Janeiro', 'Florianopolis', 'Recife'];

cidades.forEach((elemento, index) => {
    // console.log(elemento, index);
});

// for normal
for (let indicie = 0; indicie < cidades.length; indicie++) {
    console.log(cidades[indicie])
    
}

